{
  "name": "edge-quote-api",
  "version": "1.0.0",
  "description": "Cloudflare Edge Stock Quote API for A-Share and ETF real-time data",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dual": "bash scripts/deploy_dual_live.sh --deploy-worker",
    "verify:dual": "bash scripts/deploy_dual_live.sh",
    "test": "node --test tests/*.mjs tests/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
